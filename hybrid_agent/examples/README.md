Example artifacts from:

```bash
python3 -m hybrid_agent "Generate and reconstruct MNIST digits with a variational autoencoder" --out runs/mnist-vae-demo
```

Includes plan/literature/dataset/model JSON, SUMMARY.md, and inference PNGs (checkpoint omitted).
